---
import Layout from '../layouts/Layout.astro';
import { generateMeta } from 'src/utilties/generateMeta';

const metadata = await generateMeta({doc: null}) //pass in page/post once we have it

---

<Layout description={metadata.description} openGraph={metadata.openGraph} title={metadata.title}>
	hello hi
</Layout>
